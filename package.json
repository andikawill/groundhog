{
  "name": "groundhog",
  "version": "0.0.0",
  "description": "Describe an API test cycle once, then replay it with generated values.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "run-case": "tsx bin/run-case.ts"
  },
  "devDependencies": {
    "@types/node": "^20.19.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20.19"
  }
}
